requires中配置的类，将在本类之前加载。

refs配置项，会在控制器中生成getter方法，利用ComponentQuery配置对象的selector。

Application.js引入控制器，控制器中引入views models stores refs requires等。

1.把controller加载到Application.js中
2.把view引入到controller中
3.view中引入子view
4.view中绑定store
5.store中引入model
6.controller中查找组件，绑定事件，处理逻辑。

各部分的命名规则：
    名称一样，所属的文件夹不同。